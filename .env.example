# Copy file này thành `.env` để chạy local: `cp .env.example .env`
# BE local mặc định ở http://localhost:5068 (dotnet run --project FURPMS.API).
VITE_API_BASE_URL=http://localhost:5068/api
# false = gọi BE thật; true = dùng mock data (không cần BE).
VITE_USE_MOCK_API=false
