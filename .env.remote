# Dùng khi chạy `npm run dev:remote` — code FE ở máy mình, nhưng gọi BE đã deploy.
# Dành cho người KHÔNG chạy được BE local (chỉ làm FE).
# Lưu ý: BE trên Render chỉ có tới commit đã push — tính năng BE mới chưa push thì sẽ 404.
VITE_API_BASE_URL=https://furpms-be-1.onrender.com/api
VITE_USE_MOCK_API=false
